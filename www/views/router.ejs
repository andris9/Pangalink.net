<% if(page == "/account/login"){%>
    <% include login %>
<%}%>

<% if(page == "/account/join"){%>
    <% include join %>
<%}%>

<% if(page == "/account/reset-link"){%>
    <% include reset-link %>
<%}%>

<% if(page == "/account/reset-password"){%>
    <% include reset-password %>
<%}%>

<% if(page == "/account/profile"){%>
    <% include profile %>
<%}%>

<% if(page == "/add-project"){%>
    <% include add-project %>
<%}%>

<% if(page == "/edit-project"){%>
    <% include edit-project %>
<%}%>

<% if(page == "/projects"){%>
    <% include projects %>
<%}%>

<% if(page == "/project"){%>
    <% include project %>
<%}%>

<% if(page == "/payment"){%>
    <% include payment %>
<%}%>

<% if(page == "/"){%>
    <% include home %>
<%}%>

<% if(page == "/keys"){%>
    <% include keys %>
<%}%>

<% if(page == "/api"){%>
    <% include api %>
<%}%>

<% if(page == "/docs"){%>
    <% include docs %>
<%}%>

<% if(page == "/account/users"){%>
    <% include users %>
<%}%>
